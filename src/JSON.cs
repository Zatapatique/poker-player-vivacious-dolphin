namespace PokerBot
{
    internal class JSON
    {
        // public static GameState DeserializeJSONGameState(string serializedResponse)
        // {
        //     if (serializedResponse == null) throw new ArgumentNullException(nameof(serializedResponse));

        //     DataContractJsonSerializer serializer = new DataContractJsonSerializer(typeof(GameState));
        //     MemoryStream memoryStream = new MemoryStream(Encoding.ASCII.GetBytes(serializedResponse));
        //     TResponse response = (GameState)serializer.ReadObject(memoryStream);

        //     return response;
        // }
    }
}